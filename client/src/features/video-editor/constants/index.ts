/**
 * Video Editor (client) — constants.
 *
 * Client-side constants are added here as needed. Preset/threshold values remain
 * server-authoritative in `server/features/video-editor/config/`; the client
 * receives resolved values via the API rather than duplicating them.
 * Intentionally empty for the module scaffold (task 1.1).
 */

export {};
