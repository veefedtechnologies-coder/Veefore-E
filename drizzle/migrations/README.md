# Drizzle Migrations

Use `drizzle-kit` to generate and apply migrations for schema changes.

Commands:
- Generate: `npx drizzle-kit generate:pg`
- Push: `npx drizzle-kit push:pg`

Ensure `DATABASE_URL` is set for the target environment.
