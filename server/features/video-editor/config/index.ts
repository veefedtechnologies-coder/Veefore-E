/**
 * Video Editor configuration — public surface.
 *
 * Re-exports the single-source config so consumers import from a stable path:
 *   import { VIDEO_EDITOR_CONFIG, getPlatformPreset } from '../config';
 */

export * from './video-editor.config';
